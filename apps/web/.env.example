# Dev: usa el proxy /api de Vite (configurado en vite.config.ts → /api → http://localhost:4000).
# Production: setea la URL pública del backend, ej. https://api.atlasgym.cl
VITE_API_URL=/api
VITE_VAPID_PUBLIC=
